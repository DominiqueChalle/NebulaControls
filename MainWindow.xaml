<Window x:Class="NebulaUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NebulaUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">

    <Border Background="{DynamicResource Brush.Background}"
            Padding="20">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="120"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Grid.ColumnSpan="2"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Margin="0,20,0,0">

                <TextBlock Text="Welcome to NebulaUI!"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{DynamicResource Brush.TextPrimary}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Experience the beauty of WPF with NebulaUI's Dark Purple theme."
                           FontSize="14"
                           Margin="0,10,0,0"
                           Foreground="{DynamicResource Brush.TextSecondary}"
                           HorizontalAlignment="Center" />
            </StackPanel>

            <!-- Button Grid -->

            <Grid Grid.Row="1" Grid.Column="0">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Orientation="Vertical">
                    <TextBlock Text="Nebula Buttons"
                               FontSize="20"
                               Margin="0,0,0,20"
                               Foreground="{DynamicResource Brush.TextPrimary}"
                               VerticalAlignment="Top"
                               HorizontalAlignment="Center" />
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Top"
                                Orientation="Horizontal">

                        <Button Content="Primary Button"
                                Style="{StaticResource PrimaryButtonStyle}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Height="45"
                                Width="140"
                                Padding="10,5" />

                        <Button Grid.Row="1" Grid.Column="1 "
                                Content="Secondary Button"
                                Style="{StaticResource SecondaryButtonStyle}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Height="45"
                                Width="140"
                                Padding="10,5" />

                    </StackPanel>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0"
                               Text="Nebula Text Inputs"
                               FontSize="20"
                               Margin="0,0,0,10"
                               Foreground="{DynamicResource Brush.TextPrimary}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />

                    <TextBlock Grid.Row="1"
                               Text="SimpleTextBox :"
                               FontSize="14"
                               Margin="0,0,0,5"
                               Foreground="{DynamicResource Brush.TextPrimary}"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"/>

                    <TextBox x:Name="MyTextBox" Grid.Row="1"
                             Style="{StaticResource NebulaBaseTextBox}"
                             Width="260"
                             Margin="10"
                             HorizontalAlignment="Right"
                             VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="2"
                               Text="PasswordBox :"
                               FontSize="14"
                               Margin="0,0,0,5"
                               Foreground="{DynamicResource Brush.TextPrimary}"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center" />

                    <PasswordBox x:Name="MyPasswordBox" Grid.Row="2"
                                 Style="{StaticResource NebulaBasePasswordBox}"
                                 Width="260"
                                 Margin="10"
                                 HorizontalAlignment="Right"
                                 VerticalAlignment="Center"/>

                </Grid>
            </Grid>
            <Grid Grid.Row="2" Grid.Column="0">
                <StackPanel HorizontalAlignment="Center"
                            VerticalAlignment="Top"
                            Orientation="Vertical">
                    <TextBlock Text="Nebula CheckBox"
                               FontSize="20"
                               Margin="0,0,0,20"
                               Foreground="{DynamicResource Brush.TextPrimary}"
                               HorizontalAlignment="Center" />
                    <CheckBox Style="{StaticResource NebulaSimpleCheckBox}"
                              Content="CheckBox Unchecked"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,10" Grid.Row="2"
                              IsChecked="False"
                              VerticalAlignment="Center" Width="Auto"/>
                    <CheckBox Style="{StaticResource NebulaSimpleCheckBox}"
                              Content="CheckBox Checked"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,10" Grid.Row="2"
                              IsChecked="True"
                              VerticalAlignment="Center" Width="Auto"/>
                    <CheckBox Style="{StaticResource NebulaSimpleCheckBox}"
                              Content="CheckBox Indeterminate"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,10" Grid.Row="2"
                              IsThreeState="True"
                              IsChecked="{x:Null}"
                              VerticalAlignment="Center" Width="Auto"/>
                </StackPanel>
            </Grid>
            <TextBox Grid.Column="1" HorizontalAlignment="Left" Margin="33,25,0,0" Grid.Row="2" TextWrapping="Wrap" Text="TextBox" VerticalAlignment="Top" Width="120"/>
        </Grid>
    </Border>
</Window>
