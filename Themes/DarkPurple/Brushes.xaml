<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================================================== -->
    <!-- Backgrounds Brushes                                   -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.Background"
        Color="{StaticResource Color.Background}" />

    <SolidColorBrush
        x:Key="Brush.BackgroundDisabled"
        Color="{StaticResource Color.BackgroundDisabled}" />

    <SolidColorBrush
        x:Key="Brush.BackgroundReadOnly"
        Color="{StaticResource Color.BackgroundReadOnly}" />

    <!-- ===================================================== -->
    <!-- Surface Brushes                                       -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.Surface"
        Color="{StaticResource Color.Surface}" />

    <SolidColorBrush
        x:Key="Brush.SurfaceHover"
        Color="{StaticResource Color.SurfaceHover}" />

    <SolidColorBrush
        x:Key="Brush.SurfaceActive"
        Color="{StaticResource Color.SurfaceActive}" />

    <SolidColorBrush
        x:Key="Brush.SurfaceDisabled"
        Color="{StaticResource Color.SurfaceDisabled}" />

    <SolidColorBrush
        x:Key="Brush.SurfaceReadOnly"
        Color="{StaticResource Color.SurfaceReadOnly}" />

    <!-- ===================================================== -->
    <!-- Accent Brushes                                        -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.Accent"
        Color="{StaticResource Color.Accent}" />

    <SolidColorBrush
        x:Key="Brush.AccentHover"
        Color="{StaticResource Color.AccentHover}" />

    <SolidColorBrush
        x:Key="Brush.AccentPressed"
        Color="{StaticResource Color.AccentPressed}" />

    <SolidColorBrush
        x:Key="Brush.AccentDisabled"
        Color="{StaticResource Color.AccentDisabled}" />

    <SolidColorBrush
        x:Key="Brush.AccentBorder"
        Color="{StaticResource Color.AccentBorder}"/>

    <SolidColorBrush
        x:Key="Brush.AccentBorderHover"
        Color="{StaticResource Color.AccentBorderHover}"/>

    <SolidColorBrush
        x:Key="Brush.AccentBorderDisabled"
        Color="{StaticResource Color.AccentBorderDisabled}"/>

    <!-- ===================================================== -->
    <!-- Button Brushes                                        -->
    <!-- ===================================================== -->

    <LinearGradientBrush x:Key="Brush.ButtonPrimaryBorder"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.AccentPressed}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.Accent}" Offset="0.55"/>
        <GradientStop Color="{StaticResource Color.AccentBorderHover}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Brush.ButtonPrimaryBorderHover"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.Accent}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.AccentHover}" Offset="0.55"/>
        <GradientStop Color="{StaticResource Color.AccentBorderHover}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Brush.ButtonPrimaryBorderPressed"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.AccentPressed}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.AccentBorder}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Brush.ButtonSecondaryBorder"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.Border}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.BorderHover}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Brush.ButtonSecondaryBorderHover"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.BorderHover}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.BorderReadOnly}" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Brush.ButtonSecondaryBorderPressed"
                         StartPoint="0,0"
                         EndPoint="1,1">
        <GradientStop Color="{StaticResource Color.BorderDisabled}" Offset="0"/>
        <GradientStop Color="{StaticResource Color.BorderHover}" Offset="1"/>
    </LinearGradientBrush>

    <!-- ===================================================== -->
    <!-- Text Brushes                                          -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.TextPrimary"
        Color="{StaticResource Color.TextPrimary}" />

    <SolidColorBrush
        x:Key="Brush.TextSecondary"
        Color="{StaticResource Color.TextSecondary}" />

    <SolidColorBrush
        x:Key="Brush.TextMuted"
        Color="{StaticResource Color.TextMuted}" />

    <SolidColorBrush
        x:Key="Brush.TextDisabled"
        Color="{StaticResource Color.TextDisabled}" />

    <!-- ===================================================== -->
    <!-- Border Brushes                                        -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.Border"
        Color="{StaticResource Color.Border}" />

    <SolidColorBrush
        x:Key="Brush.BorderHover"
        Color="{StaticResource Color.BorderHover}" />

    <SolidColorBrush
        x:Key="Brush.BorderFocus"
        Color="{StaticResource Color.BorderFocus}" />

    <SolidColorBrush
        x:Key="Brush.BorderDisabled"
        Color="{StaticResource Color.BorderDisabled}" />

    <SolidColorBrush
        x:Key="Brush.BorderReadOnly"
        Color="{StaticResource Color.BorderReadOnly}" />

    <!-- ===================================================== -->
    <!-- States Brushes                                        -->
    <!-- ===================================================== -->

    <SolidColorBrush
        x:Key="Brush.Success"
        Color="{StaticResource Color.Success}" />

    <SolidColorBrush
        x:Key="Brush.Warning"
        Color="{StaticResource Color.Warning}" />

    <SolidColorBrush
        x:Key="Brush.Danger"
        Color="{StaticResource Color.Danger}" />

    <SolidColorBrush
        x:Key="Brush.Info"
        Color="{StaticResource Color.Info}" />

</ResourceDictionary>
