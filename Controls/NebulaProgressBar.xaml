<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:NebulaUI.Controls">

    <Style x:Key="NebulaProgressBar"
           TargetType="controls:NebulaProgressBar">
        <Setter Property="Background" Value="{DynamicResource Brush.Background}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent}"/>
        <Setter Property="MinHeight" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:NebulaProgressBar">
                    <Grid x:Name="Root"
                          MinHeight="{TemplateBinding MinHeight}"
                          SnapsToDevicePixels="True"
                          ClipToBounds="True">
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7"/>

                        <Grid Margin="{TemplateBinding BorderThickness}"
                              ClipToBounds="True">
                            <Border x:Name="PART_Indicator"
                                    HorizontalAlignment="Left"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="5"/>

                            <Border x:Name="IndeterminateIndicator"
                                    Width="92"
                                    HorizontalAlignment="Left"
                                    Background="{DynamicResource Brush.AccentHover}"
                                    CornerRadius="5"
                                    Opacity="0"
                                    RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="IndeterminateTransform"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PART_Indicator"
                                    Property="Opacity"
                                    Value="0"/>
                            <Setter TargetName="IndeterminateIndicator"
                                    Property="Opacity"
                                    Value="1"/>
                            <Trigger.EnterActions>
                                <BeginStoryboard x:Name="IndeterminateStoryboard">
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="IndeterminateTransform"
                                                         Storyboard.TargetProperty="X"
                                                         From="-96"
                                                         To="320"
                                                         Duration="0:0:1.35"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <StopStoryboard BeginStoryboardName="IndeterminateStoryboard"/>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Indicator"
                                    Property="HorizontalAlignment"
                                    Value="Stretch"/>
                            <Setter TargetName="PART_Indicator"
                                    Property="VerticalAlignment"
                                    Value="Bottom"/>
                            <Setter TargetName="PART_Track"
                                    Property="CornerRadius"
                                    Value="7"/>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"
                                    Value="{DynamicResource Brush.AccentDisabled}"/>
                            <Setter TargetName="PART_Track"
                                    Property="Background"
                                    Value="{DynamicResource Brush.BackgroundDisabled}"/>
                            <Setter TargetName="PART_Track"
                                    Property="BorderBrush"
                                    Value="{DynamicResource Brush.BorderDisabled}"/>
                            <Setter TargetName="IndeterminateIndicator"
                                    Property="Background"
                                    Value="{DynamicResource Brush.AccentDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
